<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-4 py-2 cursor-pointer" href="/logout">Logout</a>
        </div>
        <h3 class="text-white text-3xl mb-4">
            Edit Your Post
        </h3>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind" name="content"
                class="resize-none outline-none px-3 py-2 block w-1/3 border-1 border-zinc-500 rounded-md my-3"><%= post.content %></textarea>
            <input class="bg-blue-500 rounded-md px-4 py-2 cursor-pointer" type="submit" value="Update" />
        </form>
    </div>
</body>

</html>