<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-4 py-2 cursor-pointer" href="/logout">Logout</a>
        </div>
        <h3 class="text-white text-3xl mb-4">
            Hello,<span class="text-red-500">
                <%= user.name %>
            </span>
        </h3>
        <h5>You can create a new post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind" name="content"
                class="resize-none outline-none px-3 py-2 block w-1/3 border-1 border-zinc-500 rounded-md my-3"></textarea>
            <input class="bg-blue-500 rounded-md px-4 py-2 cursor-pointer" type="submit" />
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 p-4 rounded-md bg-zinc-800 border-zinc-700 border-1 mb-4">
                    <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
                    <p class="text-sm tracking-tighter"><%= post.content%></p>
                    <small><%= post.likes.length %> <%= post.likes.indexOf(user._id) > 1 ? "Likes" : "Like" %></small>
                    <div class="btns flex gap-4 mt-4">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
                <% })%>
            </div>
        </div>
    </div>
</body>

</html>